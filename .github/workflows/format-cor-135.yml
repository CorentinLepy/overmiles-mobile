name: COR-135 Formatter

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated branch-specific formatter; formatting is enforced by pnpm verify."
